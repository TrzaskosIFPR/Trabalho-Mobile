# Trabalho-WEB
Repositório referente ao trabalho final da disciplina de WEB2
Para rodar o código deve-se baixar as dependências necessárias para executar a API.
Apenas rode a API primeiro{node app.js}.
